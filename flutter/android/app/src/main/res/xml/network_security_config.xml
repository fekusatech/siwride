<?xml version="1.0" encoding="utf-8"?>
<!--
  Scopes cleartext (HTTP) traffic to known local-dev hosts only, instead of
  allowing it app-wide. ApiConfig.baseUrl defaults to http://siwride.test
  for local development (see lib/config/api_config.dart); production builds
  must point the API_BASE_URL dart-define at an https:// host, which is
  covered by the default base-config cleartextTrafficPermitted="false"
  below and needs no entry here.

  Testing on a physical device via a LAN IP override of API_BASE_URL? Add
  that IP as another domain under the dev-hosts domain-config while you
  test locally; don't add it to a release build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">siwride.test</domain>
        <!-- Android emulator's alias for the host machine's localhost. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
